# SQLite auth store
# Default seeded account on startup: admin / admin123
# Optional override path for the SQLite database file
# AUTH_DB_PATH=./server/auth.sqlite

# Allowed CORS origin for paywall page
PAYWALL_ORIGIN=https://kilby8.github.io

# File served after successful login
# Option A: redirect to an external URL (e.g. GitHub Release asset) — works on Render free tier
DOWNLOAD_URL=https://github.com/kilby8/KJ/releases/latest/download/Ironorr-Karaoke-File-System-IKFS-Setup.exe
# Option B: serve a local file (requires persistent disk)
# DOWNLOAD_FILE_PATH=./release/IKFS-Setup.exe

# HMAC secret for signing short-lived download tokens
TOKEN_SECRET=replace-with-long-random-secret

# API port and token lifetime
PORT=8787
TOKEN_TTL_SECONDS=900
